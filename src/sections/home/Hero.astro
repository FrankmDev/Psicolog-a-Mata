---
import ScrollReveal from '../../components/ScrollReveal.astro';

const stats = [
  { value: "15+", label: "Años de experiencia" },
  { value: "Córdoba", label: "Presencial y online" },
  { value: "100%", label: "Confidencial" },
];
---

<section
  class="relative min-h-[90vh] flex items-center overflow-hidden bg-bg-base pt-20 lg:pt-0"
  aria-label="Introducción"
>
  <!-- Soft gradient background element -->
  <div
    class="absolute top-0 right-0 w-[80vw] h-[80vw] lg:w-[40vw] lg:h-[40vw] bg-accent-primary/5 rounded-full blur-[100px] -translate-y-1/2 translate-x-1/4 animate-fade-in"
    aria-hidden="true"
  >
  </div>
  <div
    class="absolute bottom-0 left-0 w-[60vw] h-[60vw] lg:w-[30vw] lg:h-[30vw] bg-accent-secondary/10 rounded-full blur-[80px] translate-y-1/2 -translate-x-1/4 animate-fade-in delay-200"
    aria-hidden="true"
  >
  </div>

  <div class="relative container-elegant z-10 py-12 lg:py-0">
    <div class="grid lg:grid-cols-12 gap-12 lg:gap-8 items-center">
      <div
        class="lg:col-span-6 xl:col-span-5 order-2 lg:order-1 pt-8 lg:pt-0"
      >
        <div class="max-w-xl">
          <ScrollReveal animation="fade-left" delay={0}>
            <div class="flex items-center gap-4 mb-8">
              <div class="w-12 h-[1px] bg-accent-primary" aria-hidden="true"></div>
              <span class="eyebrow !mb-0 tracking-[0.2em]">Psicóloga Clínica en Córdoba</span>
            </div>
          </ScrollReveal>

          <ScrollReveal animation="fade-up" delay={100}>
            <h1
              class="font-serif text-[clamp(2.75rem,5vw,4.5rem)] leading-[1.1] tracking-tight text-text-primary mb-6"
            >
              Tu espacio de <span class="italic text-accent-primary font-light">bienestar</span> emocional.
            </h1>
          </ScrollReveal>

          <ScrollReveal animation="fade-up" delay={200}>
            <p
              class="text-lg lg:text-[19px] text-text-secondary leading-relaxed mb-10 font-sans max-w-md"
            >
              Acompañamiento profesional desde una perspectiva cercana
              y rigurosa. Encontremos juntos el equilibrio que buscas.
            </p>
          </ScrollReveal>

          <ScrollReveal animation="fade-up" delay={300}>
            <div class="flex flex-col sm:flex-row gap-5 mb-14">
              <a href="/contact" class="btn btn-primary magnetic" aria-label="Solicitar consulta de psicología">
                <span>Solicitar consulta</span>
              </a>
              <a href="#servicios" class="btn btn-outline" aria-label="Ver servicios de psicología">
                <span>Conocer servicios</span>
              </a>
            </div>
          </ScrollReveal>

          <ScrollReveal animation="fade-up" delay={400}>
            <div
              class="grid grid-cols-3 gap-6 pt-8 border-t border-border-light"
              role="list"
              aria-label="Estadísticas de experiencia"
            >
              {stats.map((stat) => (
                <div role="listitem">
                  <span class="font-serif text-3xl lg:text-4xl text-text-primary block mb-1">
                    {stat.value}
                  </span>
                  <span class="text-[11px] text-text-tertiary font-sans font-medium tracking-wide uppercase leading-tight block">
                    {stat.label}
                  </span>
                </div>
              ))}
            </div>
          </ScrollReveal>
        </div>
      </div>

      <div
        class="lg:col-span-6 xl:col-span-6 xl:col-start-7 order-1 lg:order-2"
      >
        <ScrollReveal animation="scale" delay={200}>
          <div class="relative max-w-md mx-auto lg:max-w-none">
            <!-- Elegant decorative frame -->
            <div
              class="absolute -inset-4 lg:-inset-6 border border-border-light rounded-t-full rounded-b-[2rem] hidden md:block"
              aria-hidden="true"
            >
            </div>

            <div
              class="relative aspect-[4/5] overflow-hidden rounded-t-full rounded-b-[2rem] shadow-card bg-bg-muted img-reveal revealed"
            >
              <img
                src="https://images.unsplash.com/photo-1573497620053-ea5300f94f21?auto=format&fit=crop&w=1200&q=80"
                alt="María Ángeles Mata - Psicóloga en Córdoba, consulta de psicología"
                class="w-full h-full object-cover grayscale-[20%] hover:grayscale-0 transition-all duration-700 hover:scale-105"
                width="600"
                height="750"
                loading="eager"
                fetchpriority="high"
              />
            </div>

            <!-- Floating badge -->
            <div
              class="absolute bottom-8 -left-6 md:-left-12 bg-bg-surface px-6 py-4 rounded-2xl shadow-card border border-border-light flex items-center gap-4 animate-fade-in delay-700"
              role="complementary"
              aria-label="Disponibilidad"
            >
              <div
                class="w-12 h-12 rounded-full bg-accent-primary/10 flex items-center justify-center shrink-0"
                aria-hidden="true"
              >
                <svg
                  class="w-6 h-6 text-accent-primary"
                  fill="none"
                  viewBox="0 0 24 24"
                  stroke="currentColor"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="1.5"
                    d="M12 21v-8.25M15.75 21v-8.25M8.25 21v-8.25M3 9l9-6 9 6m-1.5 12V10.332A48.315 48.315 0 0012 9.75c-2.551 0-5.056.2-7.5.582V21M3 21h18M12 6.75h.008v.008H12V6.75z"
                  ></path>
                </svg>
              </div>
              <div>
                <span class="block text-sm font-semibold text-text-primary mb-0.5">Consulta Presencial</span>
                <span class="block text-xs text-text-tertiary">Centro de Córdoba</span>
              </div>
            </div>
          </div>
        </ScrollReveal>
      </div>
    </div>
  </div>
</section>
