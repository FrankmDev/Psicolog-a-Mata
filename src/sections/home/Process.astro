---
const steps = [
  {
    number: '01',
    title: 'Primera sesión',
    description: 'Conozco tu historia, tus preocupaciones y qué te ha traído a terapia. Sin juicios, a tu ritmo, estableciendo una base de confianza.',
  },
  {
    number: '02',
    title: 'Evaluación',
    description: 'Juntas entendemos qué está pasando y establecemos objetivos claros para el trabajo terapéutico. Definimos el camino a seguir.',
  },
  {
    number: '03',
    title: 'Intervención',
    description: 'Trabajamos con herramientas concretas adaptadas a tus necesidades. Cada sesión es un paso hacia tu bienestar.',
  },
  {
    number: '04',
    title: 'Seguimiento',
    description: 'Revisamos tu progreso y ajustamos el camino. La terapia evoluciona contigo hasta alcanzar tus objetivos.',
  },
];
---

<section class="section bg-bg-surface border-y border-border-light overflow-hidden" id="proceso">
  <div class="container-elegant">
    <!-- Header -->
    <div class="max-w-2xl mx-auto text-center mb-16 lg:mb-20 opacity-0 animate-reveal" data-animate>
      <div class="inline-flex items-center gap-4 mb-4 px-4 py-2 bg-sage/5 rounded-full">
        <div class="w-6 h-px bg-sage/50"></div>
        <span class="eyebrow !mb-0">Metodología</span>
        <div class="w-6 h-px bg-sage/50"></div>
      </div>
      <h2 class="font-serif text-3xl lg:text-4xl text-text-primary">
        ¿Cómo funciona la <span class="italic text-sage">terapia</span>?
      </h2>
    </div>

    <!-- Steps Grid with Connecting Line -->
    <div class="relative">
      <!-- Horizontal Connecting Line (desktop) -->
      <div class="hidden lg:block absolute top-[3.5rem] left-[12.5%] right-[12.5%] h-px bg-gradient-to-r from-transparent via-sage/30 to-transparent"></div>
      
      <div class="grid sm:grid-cols-2 lg:grid-cols-4 gap-8 lg:gap-6">
        {steps.map((step, index) => (
          <div 
            class="group relative text-center opacity-0 animate-reveal" 
            data-animate 
            style={`animation-delay: ${index * 0.12}s`}
          >
            <!-- Step Number with Circle -->
            <div class="relative inline-flex mb-6">
              <div class="w-28 h-28 rounded-full bg-bg-base border-2 border-sage/20 flex items-center justify-center transition-all duration-500 group-hover:border-sage group-hover:shadow-lg group-hover:shadow-sage/10">
                <span class="font-serif text-4xl text-sage/30 group-hover:text-sage transition-colors duration-300">
                  {step.number}
                </span>
              </div>
              <!-- Connection dot -->
              <div class="hidden lg:block absolute -bottom-2 left-1/2 -translate-x-1/2 w-3 h-3 rounded-full bg-sage/20 group-hover:bg-sage transition-colors"></div>
            </div>
            
            <!-- Content -->
            <h3 class="font-serif text-xl text-text-primary mb-3 group-hover:text-sage transition-colors duration-300">
              {step.title}
            </h3>
            <p class="text-text-secondary text-sm leading-relaxed max-w-[260px] mx-auto">
              {step.description}
            </p>
          </div>
        ))}
      </div>
    </div>

    <!-- Bottom CTA -->
    <div class="mt-16 text-center opacity-0 animate-reveal" data-animate style="animation-delay: 0.5s">
      <p class="text-text-secondary mb-5">¿Listo para dar el primer paso?</p>
      <a href="/contact" class="btn btn-primary">
        <span>Reservar primera sesión</span>
        <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M17.25 8.75L21 12m0 0l-3.75 3.25M21 12H3" />
        </svg>
      </a>
    </div>
  </div>
</section>

<script>
  if (typeof window !== 'undefined') {
    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            entry.target.classList.add('animate-reveal');
            observer.unobserve(entry.target);
          }
        });
      },
      { threshold: 0.1, rootMargin: '0px 0px -50px 0px' }
    );

    document.querySelectorAll('[data-animate]').forEach((el) => {
      observer.observe(el);
    });
  }
</script>
